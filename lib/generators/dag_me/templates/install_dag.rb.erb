# frozen_string_literal: true

class InstallDagMeFor<%= class_name.pluralize %> < ActiveRecord::Migration<%= migration_version %>
  def up
    DagMe::DDL.install!(<%= class_name %>)
  end

  def down
    DagMe::DDL.uninstall!(<%= class_name %>)
  end
end
